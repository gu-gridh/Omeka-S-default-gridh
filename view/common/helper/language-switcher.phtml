<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SiteRepresentation $site
 * @var string $defaultLocale The default locale set in main settings or config (xx_YY).
 * @var array $locales
 * @var array $localeLabels
 * @var string $displayLocale "code" (the 2 or 3 letters language code, default) or "flag".
 */

// No switcher if there is only one site or a single language.
if (count($locales) <= 1) {
    return;
}

$currentLocale = $this->siteSetting('locale') ?: $defaultLocale;
$escapeAttr = $this->plugin('escapeHtmlAttr');
$escapeHtml = $this->plugin('escapeHtml');
?>

<ul class="language-switcher locale-<?= $escapeAttr($displayLocale) ?>">
    <?php if ($displayLocale === 'flag'): ?>

    <?php $localeToCountry = $this->plugin('localeToCountry'); ?>
    <?php foreach ($locales as $locale): ?>
        <?php $country = $localeToCountry($locale['locale']); ?>
        <li>
            <?php if ($currentLocale == $locale['locale']): ?>
                <span class="active flag-icon flag-icon-<?= $escapeAttr(strtolower($country)) ?>" title="<?= $escapeAttr($locale['locale_label']) ?>"></span>
            <?php else: ?>
                <a href="<?= $escapeAttr($locale['url']) ?>" title="<?= $escapeAttr($locale['locale_label']) ?>"><span class="flag-icon flag-icon-<?= $escapeAttr(strtolower($country)) ?>"></span></a>
            <?php endif; ?>
        </li>
    <?php endforeach; ?>

    <?php else: ?>

    <?php foreach ($locales as $locale): ?>
        <li>
            <?php if ($currentLocale == $locale['locale']): ?>
                <span class="active" title="<?= $escapeAttr($locale['locale_label']) ?>"><?= $escapeHtml(rtrim(substr($locale['locale'], 0, 3), '_-')) ?></span>
            <?php else: ?>
                <a href="<?= $escapeAttr($locale['url']) ?>" title="<?= $escapeAttr($locale['locale_label']) ?>"><?= $escapeHtml(rtrim(substr($locale['locale'], 0, 3), '_-')) ?></a>
            <?php endif; ?>
        </li>
    <?php endforeach; ?>

    <?php endif; ?>
</ul>